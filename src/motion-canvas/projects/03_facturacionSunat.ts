import {makeProject} from '@motion-canvas/core';
import scene from '../scenes/facturacionSunatScene?scene';

export default makeProject({
  name: '03 - Facturacion SUNAT (10 Pasos)',
  scenes: [scene],
});
